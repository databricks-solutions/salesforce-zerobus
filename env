# Table Configuration
DEFAULT_POSTGRES_SCHEMA=public
DEFAULT_POSTGRES_TABLE=orders_synced

LAKEBASE_INSTANCE_NAME=gdoyle-fastapi-demo # Name of the Lakebase instance
LAKEBASE_DATABASE_NAME=gdoyle-database # Name of the Lakebase database
LAKEBASE_CATALOG_NAME=gdd-lakebase-catalog # Name of the Lakebase catalog
SYNCHED_TABLE_STORAGE_CATALOG=grant_doyle # Catalog where you have permissions to create tables. Used to store metadata from the lakebase synced table pipeline.
SYNCHED_TABLE_STORAGE_SCHEMA=lakebase_sme_demo # Schema where you have permissions to create tables. Used to store metadata from the lakebase synced table pipeline.

# Authentication Mode
# Set to 'true' to enable per-user authentication using Databricks Apps headers
# Set to 'false' for app-level authentication (default)
USER_BASED_AUTHENTICATION=false

DATABRICKS_DATABASE_PORT=5432
# Database Connection Pool Settings
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_COMMAND_TIMEOUT=30
DB_POOL_TIMEOUT=10
DB_POOL_RECYCLE_INTERVAL=3600